{
    "name": "openvision-face-detector",
    "version": "1.0.0",
    "description": "Unified orchestration for OpenVision Face Detector",
    "scripts": {
        "dev": "bash scripts/dev-orchestrator.sh",
        "install:all": "npm install && cd web-dashboard && npm install && cd ../backend && bash scripts/setup_env.sh",
        "setup": "bash scripts/dev-orchestrator.sh --setup-only"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}